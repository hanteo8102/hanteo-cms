{
  "routes": [
    {
      "method": "GET",
      "path": "/apps/news-contents",
      "handler": "app.findNewsContents",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/apps/auth/confirm-check",
      "handler": "app.confirmCheck",
      "config": {
        "policies": []
      }
    }

  ]
}
